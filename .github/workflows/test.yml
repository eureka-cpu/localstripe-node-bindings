name: Tests
on:
  push:
  pull_request:
jobs:
  nixos-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - run: nix-build -A passthru.tests
